public class Paser {
    private Token token;
    private TokenTable table;
    private PaserUnit compUnit;

    /*public Paser(TokenTable tokenTable) {
        table = tokenTable;
        compUnit = compUnit();
    }

    /*public PaserUnit compUnit() {
        token = table.getCurToken();
        while (token.getType().equals("CONSTTK")) {

        }
    }*/
}
